# deployed by ansible!

# Don't display the copyright page
startup_message off           # default: on

# keep scrollback n lines
defscrollback 2000 


# disable password locking, accidental.
# https://superuser.com/questions/343965/what-is-the-default-password-of-screen/831140
bind x
bind ^x


# issue with less using italics
# https://unix.stackexchange.com/questions/179174/make-less-highlight-search-patterns-instead-of-italicizing-them
# to my ~/.screenrc and it does set the TERM to "screen-256color"
# The difference for me is that TERM is being set to "screen" when in tmux, but "xterm-256color" when not.
term xterm-256color

